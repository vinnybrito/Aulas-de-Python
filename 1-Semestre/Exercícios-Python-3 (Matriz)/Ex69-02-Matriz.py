# 69- Entrar via teclado com doze valores e armazená-los em uma matriz de ordem 3x4. 
# Após a digitação dos valores solicitar uma constante multiplicativa, que deverá 
# multiplicar cada valor matriz e armazenar o resultado em outra matriz de mesma 
# ordem, nas posições correspondentes. Exibir as matrizes na tela, sob a forma 
# matricial, ou seja, linhas por colunas.

matriz = []
matriz2 = []

linhas = int(input("Digite a quantidade de linhas que a matriz terá: "))

colunas = int(input("Digite a quantidade de linhas que a matriz terá: "))

